# AliasAgent

## 项目概述

Flutter 桌面 AI 对话应用，通过 dart:ffi 调用 C++ Sidecar 动态库（Anthropic API + 工具执行）。

## 关键规则

- **自己看日志** — 排查问题需要日志时，直接去 `%USERPROFILE%\.aliasagent\logs\sidecar.log`（Windows）或 `~/.aliasagent/logs/sidecar.log` 读取，不要叫用户去看。
- **唯一的 TODO 文档** — 项目所有待办统一记录在根目录 `TODO.md`（唯一载体，位置在本文件此处登记）。**禁止在其它路径另建 TODO / TODOList / 待办 等文档**；新增、更新、勾选待办一律改这一份。与 `docs/known-bugs.md`（已确认缺陷）区分：`TODO.md` 装“尚未开始 / 已立项待做”的事项。
- **不要写 STOP HERE 门禁** — tasks.md 中不要在 phase 之间插入 `⛔ STOP HERE` 标记。任务应写为连续的 checklist，一口气全部执行。
- **tasks.md 序号 = 执行顺序，不是摆设** — tasks.md 是执行任务的唯一载体，openspec 按文件物理顺序解析 `- [ ]`/`- [x]` 依次执行，**任务序号/排列必须严格反映执行顺序（前置在文件前部、后继在后部）**。新增任务、返工任务、审查发现的新任务**必须按执行顺序插入合适位置**：作为某任务前置的先插到它前面，作为后继的放它后面；**禁止**一股脑追加到文件末尾——末尾只放真正无前置/后继依赖的自由任务。执行顺序完全由序号决定，不靠人记忆"哪个先做"；一旦发现某条任务的本该在前却排在了后面，先修正排列顺序，不要靠备注补救。
- **代码必须对应 tasks.md 任务（写无任务的代码 = 重罪，且必须回退）** — 执行 OpenSpec change（`/opsx:apply`）期间，写任何代码（工具 / UI / 错误路径 / 脚本改动）**动手前必须在 tasks.md 有对应任务**（`- [ ]`/`- [x]`），并按执行顺序落到合适位置，才允许实现。**严禁"先写码、做完再回头补任务"**——那是重罪（用户实锤过）。任务必须先落（proposal 级详细、可自测、有依赖顺序）再写码；spike（验证做法值不值得）属设计探索**不入 tasks**（见 [[feedback-spike-not-a-task]]）。**若已违规（没先加 tasks 就改了代码），必须手工把该批无任务代码回退到改代码前的基线**，再作为对应任务干净重写；**禁止**补个任务就保留原代码混过去、或以"已经在改了"为由继续——回退是违规的强制纠正，不是可选项。
- **调试参考** — 排查 C++ sidecar 问题时，参考 `DEBUGGING.md` 了解日志级别、崩溃诊断、API 错误日志、FFI 追踪和 ASan 构建模式。
- **测试规范参考（权威）** — 测试怎么写/放哪层/什么标准，一律以 `docs/TESTING.md` 为权威（分层决策、怎样写、实现规范、调试读取、可观测性 §3.1、报错处理 §3.2）。**新增任何功能（含工具/UI/错误路径/脚本改动）必须配套对应层测试**——能纯逻辑→`test/unit`、渲染/交互→`test/widget`、假 sidecar 端到端→`test/integration`、真实模型端到端→`integration_test`+`--tags live`、C++→`sidecar/test`、整套冒烟→`test/smoke/run_all.sh`（分层决策见 TESTING.md §1），并在 tasks.md 落到可自测的验证任务。既有确认缺陷在 `docs/known-bugs.md`。
- **外部状态判定必用产品代码，禁止自造速查脚本** — 判定“config 是否完备 / 某 provider / 外部能力是否可用”这类真实结论，必须复用**产品代码的解析路径**（读 `lib/models/*.dart` 确认 JSON 键映射，或直接调用 `ConfigService.load()` / `getSearchProviders()` / `browser_available()` 取结果），**禁止**手写 python/grep 速查脚本凭印象写字段名去解析（典型教训：config 真实键是 `agent_types` 下划线，曾误写 `agentTypes` 驼峰 → 反复误报“无 general config” → 误让 live 测试跳过）。规则：
  1. **负面结论先复核** — 凡“缺失 / 不可用 / 无 key / 为空”的负面结论，先复核依据再下结论；负面结论最易因查错键名/路径而误报。
  2. **与用户说法矛盾时默认用户对** — 结论与用户陈述/直觉冲突时，先停下把自己依据查实，不坚持可能查错地方的脚本。
  3. **“外部缺失”写进审查/诚实披露前必先经产品代码验证** — 否则会让一个可避免的技术误判被诚实性流程背书。
- **禁止修改验收标准** — 不得为了通过验收而修改测试代码、tasks.md、spec、design 或其他验收标准文件，除非经过用户明确允许。包括且不限于：隐藏测试用例、删除失败测试、批量打勾、降低断言标准、修改 spec 使代码"符合"规范。
- **测试输出可观测性（所有测试的基本要求）** — 任何测试的输出都必须让运行者/审查者**知道测试实际做了什么**，禁止只输出 `OK`/断言失败而看不到测试内容。具体要求：
  1. 模型驱动类测试（live / integration_test，真实模型套件）的输出必须呈现**实际工具调用**（工具名、完整 input、result/status）与**文件最终状态**；现场在**断言前**输出，使失败时也能看到发生了什么。
  2. 测试失败必须有**可归因的现场证据**（该轮实际行为 + 文件内容 dump），禁止"失败但日志无现场"；无法从输出解释归因的失败视为规范缺陷。
  3. 观察通道随测试形态不同（headless harness 直接持有工具调用记录；窗口版经 UI 卡片 `ToolCallCard.activity` 读取），但**无论哪种形态，测试内容都必须可观察**——拿不到测试内容即规范违规。
- **apply 期间禁止无故停顿** — `/opsx:apply` 执行期间，连续执行所有任务直到全部完成或遇到阻断性阻塞。**禁止**在以下情况停顿：完成一批任务后汇报进度、询问用户"接下来做什么"、展示当前状态、等待用户确认。**唯一合法停顿条件**：遇到必须用户亲自决策的阻断性问题（如环境配置缺失、网络不通、API 密钥无效等导致后续任务无法继续）。停顿时不汇报进度，只说明阻塞点和需要用户做什么。全部任务完成后，用户看到的最后一条消息必须是诚实性审查报告，不得是闲聊、总结、或确认。
- **用 `/opsx:propose` 生成完整 change，禁止手写零碎 artifacts** — 创建任何 OpenSpec change 必须通过 `/opsx:propose` 一次生成全套 artifacts（proposal.md + design.md + specs deltas + tasks.md），**禁止**手写单个 proposal.md 再零碎补其余。审查/设计/apply 都必须在**完整 change** 上做，不得对孤立的 proposal.md 反复审查。OpenSpec 流程由主循环驱动到底，不得用"下一步你定 / 要我继续吗 / 两件事你选一个"把决定权推回用户。**反思必须落地**：自省出的错误（偏离要求、流程违规等）必须写入 memory 或本条/相关规则后才算完成，否则等于没反思。
- **openspec 下禁止存在只有 proposal 的活跃 change** — 定义与边界必须精确：
  - **判定范围（垃圾只在这里查）**：`openspec/changes/<name>/` 的**顶层直接子目录且含 `.openspec.yaml` 标记**的活跃 change。判据用 `openspec status --change <name> --json`：`tasks` 为 `blocked`（missingDeps 缺 design/specs，即只有 proposal 或 artifact 集不全）→ 垃圾。
  - **删除动作**：`rm -rf openspec/changes/<name>` 后用 `/opsx:propose <name>` 重来；不修补、不保留。
  - **永不删（不在判定范围）**：`openspec/changes/archive/` 及其下全部内容——无论其中是否有只有 proposal.md 的目录（实存案例：`archive/2026-05-14-explore-agent-framework` 只有 proposal.md 且无 `.openspec.yaml`，是合法归档历史）。禁止用"目录里文件少"这类感性判据去删归档。未含 `.openspec.yaml` 的目录不是活跃 change，不适用本规则。
- **开 Workflow 后不要等** — 启动 Workflow（或任何后台任务）后，**禁止**用 TaskOutput 阻塞等待或空转等待。Workflow 完成会自动通知（task-notification），收到通知后再处理结果。等待期间如果有可推进的工作就继续做，没有就结束当前回复，等通知。
- **Git 提交需要明确授权** — 不得在用户没有明确说"提交/commit/交"的情况下执行 git commit/amend/reset/rebase。"任务做完了"不是提交授权。提交内容必须干净，不夹带调试文件或未确认的改动。
- **审查必须验证外部真实性（完全参考本地文档）** — 所有审查（包括 Workflow 对抗验证）必须检查"外部真实性"：审查对象声称依赖的任何 API 格式、参数、字段、行为，必须能由**本地官方文档**（`Docs/DeepSeekAPIDoc.md`、`Docs/AnthropicAPIDoc.md` 等，由官方文档整理而成）验证。规则：
  1. 审查输入必须包含项目实际使用的 provider/API 上下文（如 DeepSeek Anthropic 兼容端点），禁止用错误的 provider 标准（如纯 Anthropic 标准）审查实际为 DeepSeek 的项目。
  2. **Workflow agent 禁止使用任何需要访问互联网的功能** — 包括但不限于 MCP 工具（`mcp__web-reader__webReader`、`mcp__web-search-prime__web_search_prime` 等）、WebSearch、WebFetch、以及任何网络请求（curl/HTTP 抓取）。外部真实性**完全参考本地文档**（`Docs/*.md`）。本地文档中没有记载的接口/参数/字段 = 未验证，不得作为结论采纳；审查报告必须指明所依据的本地文档章节并附原文引用。
  3. 审查报告必须为**每个接口/参数/字段**指明其引用的本地文档章节，并附原文引用（直接引用文档中的 JSON 或文字）；本地文档无记载者，该接口视为未验证，不得作为结论采纳。
  4. 审查必须对照用户原始需求，不得只对照被修改后的 artifacts——若需求曾被修改，审查需检查修改是否有用户明确授权，无授权即视为擅自降级。
  5. **拒绝幻想的接口和功能** — 任何声称存在的 API 参数、字段、端点行为，若无法用本地文档原文验证，一律视为幻想，不得写入实现、spec、design 或作为审查结论采纳。
  6. **对抗验证者必须核对本地文档原文** — 对抗验证阶段，对抗者必须亲自用 Read 读 `Docs/*.md` 核对所声称的接口/参数/字段（原文存在，不是断章取义或幻觉）。核对通过才可判定该外部依据成立。
  7. 主循环不得把 workflow 输出当作真理直接执行——外部事实类结论（API 格式、行为、限制）必须自己核对本地文档后才可采纳或落地。
  8. **本地文档的维护** — 主循环负责用 WebSearch/WebFetch 从官方文档更新 `Docs/*.md`（用户明确授权时）：审查前若发现本地文档缺少相关章节，先补充文档再开审查；发现本地文档与官方不一致时，以官方为准更新文档并记录。文档头部须标注抓取/更新时间与来源 URL。
- **tasks 诚实性审查循环** — tasks.md 的最后一项任务必须是诚实性审查任务。规则：
  1. 审查已勾完成 `[x]` 的任务，找出虚假完成、偷懒、跳过步骤、降低标准等问题
  2. **所有审查任务必须开 Workflow 做对抗验证**（多 agent 从不同维度独立审查，交叉验证发现的问题）。**无例外**：包括首轮审查、后续轮次审查、以及超过 3 轮上限后的最终收尾审查。**永远禁止手动审查替代 Workflow**
  3. 验证完成后，将发现的问题追加为新任务（**按执行顺序插入合适位置**：若该修复是后续任务的前置则插入其前，非一概末尾——见"tasks.md 序号 = 执行顺序"规则）。追加完成后**不得做任何汇报、总结或等待**，必须直接开始修复新增任务。**以下节点不是暂停点**：Workflow 完成、tasks.md 更新、审查任务追加、一轮修复结束。唯一可暂停的节点：所有 `[ ]` 任务已全部修复为 `[x]`，或达到 3 轮上限且残余任务已全部修复
  4. 新任务的最后一项仍然是诚实性审查任务
  5. 反复循环，直到诚实性审查确认没有问题，**或者达到 3 轮上限**（即最多开 3 轮 Workflow 审查）。达到上限后仍需追加一轮 Workflow 收尾审查，且该轮发现的问题必须全部修复
  6. **绝对禁止把已打勾 `[x]` 的任务改回 `[ ]`** — 已完成的任务标记不可逆转。发现问题只能追加新任务，不能撤销旧任务的勾选状态。
  7. **停止时必须输出审查结果报告** — 无论因何种原因停止（全部完成或达到上限），最后一条消息必须是诚实性审查的报告，包含：已审查轮数、每轮问题数、最终任务状态。不得以任何其他内容（闲聊、解释、确认）作为最后输出。
  8. **回归检查（防改来改去）** — 每轮审查（含收尾审查）必须检查**前几轮已修复的问题是否回归**：Workflow 审查输入必须携带历轮审查追加的问题清单及其修复状态，对抗验证 agent 须逐一验证这些修复在当前代码中仍然成立（未被本轮修复破坏）；发现回归 → 追加为新的修复任务。禁止以"之前已修过"为由跳过回归验证或忽略回归发现。
