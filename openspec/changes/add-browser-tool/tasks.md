# Add Browser Tool — Tasks

## 1. 前置验证（连续 checklist，非 STOP 门禁；结果回填 spec，供 2.4/5.3 等后续任务依赖）

- [x] 1.1 核实 lib/main.dart 的 `tool_use` → 工具执行循环已支持足够深的多步（构造：`while(true)` 循环、`_executeTool` 派发、tool_result 作为 user 消息回喂、50-turn cap；均位于 Flutter 客户端 lib/main.dart；apply 时实测当前行号 `while(true)`@998、`_executeTool` 调用@1338、`tool_use_id` 回喂@1372、cap@1423，行号会随后续改动漂移故以构造描述为准）。**结论：PASS**——多步前提成立，无需新增前置。若某前提不满足，按「tasks 序号=执行顺序」新增**前置任务**实现之，不降级为单步；把结论记入 spec 的独立浏览器要求。（注：循环在 Flutter 客户端 main.dart，非 C++ sidecar。）
- [x] 1.2 实测 headed 驱动系统 Edge：每 tab 资源（RAM / 冷启动）；**窗口最小化后 AI 连续 navigate/click/snapshot 是否仍成功执行且快照仍正确/新**（不只是「是否被拉回前台」）；据结果记录是否需加 `--disable-backgrounding-occluded-windows`，**两类分支结论都回填 spec 对应 Requirement**。**结论（2026-09-06 spike）**：资源——空页 ~414MB、3000 节点页 ~459MB、第 2 个 tab +~63MB、冷启动 ~0.33s（RAM 414-459MB 在设计 300-500MB/tab 内；冷启动**快于** design 2-5s 估计）；**最小化分支 PASS**（minimize → navigate/click/snapshot 成功、快照保持新、windowState 保持 minimized 未被拉回）；**遮挡分支（被另一窗口覆盖，非最小化）未实测**，故 `--disable-backgrounding-occluded-windows` 对**遮挡路径**的**必要性仍未验证**——按 IF/THEN 门禁，遮挡路径在 task 1.5 确认前**不视为离屏执行有保证**。（注：早期未过滤读数 ~1GB 为首绘瞬时峰值，稳态取 ~459MB。）
- [x] 1.3 spike：确认 `channel="msedge"`/`"chrome"` 能否不经 `playwright install` 直接驱动系统 Edge（免 ~150MB Chromium 下载）；若只能捆绑 Chromium，须重估资源成本。**结论（2026-09-06 spike）**：`channel="msedge"` 驱动**系统 Edge**（Browser.getVersion product=`Edg/151.0.4129.72`，5 个新进程均为 `Program Files (x86)\Microsoft\Edge\...\msedge.exe`），bundled `chromium-1228` 未被启动 → **免下载成立，无需重估资源成本**。**残差 caveat**：本机已有 crawl4ai 装的 chromium，「全新机器零安装」未直接复现，但 channel→系统二进制、捆绑未用 的机制即免下载来源。
- [x] 1.4 实测 tab/弹窗/下载/权限抑制：驱动一个会 `window.open`/`target=_blank` + 触发下载/权限弹窗的页面，记录所选 launch args + 显式 popup/新页关闭 handler + deny-downloads/permission-denial 配置是否能保持**单 tab / 无新窗 / 无弹窗 / 无下载 / 无权限授予**。**结论（2026-09-06 spike）**：`window.open` 与 `target=_blank` 经显式 `context.on("page")` close handler 均回到 **1 tab**；`alert`/`confirm` 自动 dismiss、页面存活；download 经 `dl.cancel()` 或 `accept_downloads=False` 均**无文件落盘**；**权限抑制在 file:// 上无法触发（`window.__geo`/`__notif` 返回 null）→ 未验证**，需真实 HTTPS 安全上下文页。回填 spec 的 stay-hidden 要求与 tasks 2.4 机制；权限抑制由 task 1.6 补测。
- [x] 1.5 实测**遮挡分支**（窗口被另一窗口覆盖，**非最小化**）下 navigate/click/snapshot 是否仍成功且快照仍新/准，以及是否需 `--disable-backgrounding-occluded-windows`。**结论（2026-09-06 spike）**：用不透明顶层全屏窗盖住浏览器 → navigate/click/snapshot **全部成功**、快照**保持新**（`OCC-SENTINEL-3` 正确读到）、rAF 帧率 `123→123`（**无渲染节流**）→ 覆盖窗**不破坏离屏执行**，**无需** `--disable-backgrounding-occluded-windows`。**诚实 caveat**：遮挡窗为合成全屏顶层窗；未单独读 Chromium 内部 occluded 标记，但「操作成功 + 帧率不变」的运行信号已回答特性问题。结论回填 spec 的离屏执行要求。（供 2.4 实现与 5.3 验收。）
- [x] 1.6 在真实 HTTPS 安全上下文页（非 file://）驱动一个触发权限提示（geolocation / notification）的页面，实测 deny-permission 配置能否保持不授予权限、无权限弹窗。**结论（2026-09-06 spike，permission.site HTTPS 安全上下文）**：Playwright 自动化**默认自动拒权**——Notifications 点击后 `permissions.query` 变 `denied`、直接 `Notification.requestPermission()` 返回 `denied`；Geolocation 直接 `getCurrentPosition` 返回 `error:3`（TIMEOUT，非 permission-denied——6 秒测试超时内取不到位置）、`permissions.query` 保持 `prompt`（**未授予**）。→ 「不授予权限」**成立**；「无权限弹窗」**仅 Notifs 路径确认**（Notifications `denied`；Geolocation 未授予、`error:3` TIMEOUT、state `prompt`，**未演示无弹窗**）。结论回填 spec 的权限抑制要求。（供 2.4 实现与 5.3 验收。）

## 2. Playwright 浏览器 worker（Python）

- [x] 2.1 新建 Python worker（镜像 `scripts/fetch_worker.py` 的 stdin/stdout JSON 协议），Playwright `channel="msedge"`（回退 `chrome`）、`headless=False` 启动。**已实现于 `scripts/browser_worker.py`**（长驻 stdin/stdout JSON、`channel` 探测回退 `chrome`、`headless=False`；spike 已确认 msedge 可用、chrome 本机缺失故回退）。
- [x] 2.2 实现**持久会话**（一次任务一个 daemon；`open` = 会话启动；`relaunch` = **AI 显式重开**，看门狗只检测/返回失败不自动重启，见 spec 健壮性），命令集 `browser_navigate` / `browser_click` / `browser_type` / `browser_snapshot`（worker stdin 协议用裸 `{"cmd":"navigate"/"click"/"type"/"snapshot"}`）。**已实现**：长驻循环逐条读 stdin；`open`/`relaunch` 内部命令 + `navigate`/`click`/`type`/`snapshot`；看门狗：`is_connected` 检测 + 先`dead:true` 返回一次、下次 command 才重开（AI 显式重开，绝不静默隐藏）。
- [x] 2.3 `browser_snapshot` 返回**文本快照**（DOM `innerText` / 可读性提取），带长度上限/简要分块以控 token；worker 对每个 browser 工具的**每步调用**返回一个**可观测记录**（toolName / 完整 input / result / status / 快照长度 + **工具自己 emit 的 bringToFront 次数**、**popup/新页关闭次数**、**download-denied**、**permission-denied**、**当前 tab 数**），供测试归因（这些是工具自身动作/计数，非 OS 窗口态）。**已实现**：每条命令写 `browser-record: {...}` 到 stderr（→sidecar.log，非模型可见）；快照 `MAX_SNAPSHOT_CHARS=12000` 封顶带 `truncated`；记录含 tool/input/ok/status/snapshot_len/raise_count/popup_closed/download_denied/permission_denied/permissions/tabs/divergent/error。
- [x] 2.4 **藏后台行为**：不 `bringToFront`（并记录上述“工具 emit 次数”，独立于 OS/browser focus 事件）、单 tab 复用（含显式 popup/新页关闭 handler）、自动处理 `dialog`、deny 下载（1.4 已确认）/权限弹窗（权限抑制已由 1.6 实测：不授予——Notifs denied、Geoloc 未授予；无弹窗仅 Notifs 路径）；遮挡分支的离屏执行保证已由 1.5 实测（覆盖窗不破坏 ops）；仅通过「显式可见请求」机制才提升前台——**该机制未在 design 定义**（见 design Open Question 5；需另行立项），本 change 只承诺工具自身不因逐动作 raise。**已实现**：从不 `bring_to_front`（raise_count 恒 0）、`context.on("page")` 关弹窗/新页（单 tab）、`on("download")` + `cancel()` deny 下载、权限不授予（靠 Playwright 自动拒权 + 按步读 `navigator.permissions` 观测）、`dialog` 靠 Playwright 默认自动 dismiss。
- [x] 2.5 **健壮性**：看门狗检测浏览器关闭、每步重读实时快照、URL/内容分叉检测、用户接管信号。**已实现**：看门狗见上；每 command 重新 `inner_text` 实时快照；`_expected_url` 比对 URL 变化 → `divergent`/`note`（用户接管信号）。

## 3. Sidecar 集成

- [x] 3.1 sidecar 新增浏览器工具注册 + `tool_use` 派发；复用 subprocess 代理 / 超时 / stderr→sidecar.log（SSRF 防护不作为逐交互步复用，见 design:17）；提供 `browser_navigate -> {"cmd":"navigate"}` 等一一映射。**已实现**：新增 `sidecar/src/browser.cpp`/`browser.h` + `sidecar_api.cpp` 导出 `browser_navigate/click/type/snapshot`+`sidecar_api.h` 声明；browser_op_body 把 FFI 请求参数透传给 worker `{"cmd":...}` 并透传 worker 响应（模型可见只含 snapshot）；stderr 排水线程把 worker 每行（含 `browser-record:`）原样写 sidecar.log；持久 worker 未运行时不静默重启——返回 `dead:true` 让 AI 显式重开。
- [x] 3.2 sidecar 提供 `browser_available()` 探测（返回 Playwright + 可用 Edge/Chromium 是否就绪），供 Flutter 端决定是否声明浏览器工具。**已实现**：`browser_available()`（FPPI 导出 + `browser::browser_available()`）——用 `subprocess::run` 一次性跑 `browser_worker.py {"cmd":"available"}`（worker 探测后退出），返回 `{"ok":true,"available":bool,...}`；Dart 侧 `SidecarBridge.browserAvailable()` isolate 派发。
- [x] 3.3 优雅降级：Playwright/Edge 不可用时，该工具**不声明**或返回可读错误，**绝不静默占位**。（注：`web_fetch`/`web_search` 都在 `if (hasProviders)` 内声明（`lib/main.dart:526-585`，受 provider 可用性门控）；`web_fetch` 缺 Python 时**降级到 curl fallback**（`web_fetch.cpp:96,670-672`）而非不声明——与本 change 的“浏览器运行时不可用则不声明/可读错误”不同，且浏览器工具应在 `if (hasProviders)` **之外**声明。）**已实现**：browser_available==false 时 Flutter 不把浏览器工具加进 `_toolDefs`（缺席而非占位）；browser.cpp 在 python/playwright/Edge 缺失时返回可读错误 `{"ok":false,"error":...}`（绝不返回成功占位）；非 Windows 平台返回 `browser tools not supported`（优雅降级）。

## 4. Flutter 声明

- [x] 4.1 `lib/main.dart` 在 `_toolDefs`/`toolsJson` 声明浏览器工具，**放在 `if (hasProviders)`（`lib/main.dart:526-585`）之外**，由 `browser_available()`（task 3.2）决定是否加入——确保 `hasProviders==false`（如仅 SearXNG 无 key）时浏览器工具**仍声明**（只要浏览器运行时就绪）。浏览器状态的 UI 展示（`ToolCallCard`/真实上下文视图）**不在本 change**，另行立项。**已实现**：`_initSearchAndTools()` 在 `final base={...}` 结束后、search 初始化之前 `await _sidecar.browserAvailable()`；available==true 时向 base 加 `browser_navigate/click/type/snapshot`（hasProviders 块之外）；`_executeTool` 增加四 case 派发 + content 设为 snapshot（url/title/divergence-note）+ 计数不进模型可见文本。
- [x] 4.2 增加 gate 解耦测试：`hasProviders==false`（provider 空/仅 SearXNG 无 key）+ 浏览器就绪 → 浏览器工具**仍在** `_toolDefs`；Playwright/Edge 不可用 → 浏览器工具**缺席**，而 `web_search`/`web_fetch` 仍只受 provider 门控。断言前/失败路径前 `[OBS]` dump `_toolDefs` 是否含浏览器工具。**已实现**：`test/integration/browser_gate_decoupling_test.dart`（FakeSidecar 注入）——Case A `getSearchProviders=[]`+`browserAvailable=available:true` → `sentToolNames` 含 `browser_navigate/click/type/snapshot` 且不含 `web_search/web_fetch`；Case B `browserAvailable=available:false` → 浏览器工具缺席。每 case `[OBS]` dump 实际 `sent toolsJson` 与解构工具名。**实际跑过：两 case 均通过**（见运行输出 `[OBS] deconstructed tool names`）。

## 5. 测试（匹配层 + 可观测性）

- [x] 5.1 worker 层测试：`browser_navigate`/`browser_click`/`browser_type`/`browser_snapshot` + 藏后台行为；**输出可观测**（每个 case 在断言前 `debugPrint` 实际发出的命令名 / 完整 input / 返回 result，或用 Fake worker 记录）；**每条失败路径**（worker 错误 / 超时 / 结果异常 / Fake worker 抛错）在 `fail`/`markTestSkipped` 或异常传播前 dump（或确认该轮命令/input/result 已完整落盘的 Fake worker 记录），[OBS] 现场打印，确保失败可归因。Fake worker 分支仅用于非 UI 管线，不用于最小化/分叉检测。**已实现**：`test/integration/browser_worker_test.dart` 直接 spawn `scripts/browser_worker.py`，驱动 available/navigate/type/click/snapshot；每命令先 `[OBS] worker cmd` 后 `[OBS] <op> result`（命令名/完整 input/result）；失败路径 `timeout(45s)` 返回错误 JSON；读 stderr `browser-record:` 断言 `raise_count==0`、`tabs==1`、计数不含 `snapshot` 键。**实际跑过：2 测试全过**（available 探针 + 四命令持久/藏后台）。
- [x] 5.2 sidecar 工具测试：注册/派发、不可用时降级；**断言前与每条失败路径**（注册/派发失败、超时）在 `fail`/`markTestSkipped` 之前 `debugPrint` 实际工具调用（工具名 / 完整 input / result/status）或落盘记录，[OBS] 现场打印；**降级到“工具不存在”**（spec 优雅降级）时 dump 该“可读错误/未声明”状态而非工具调用（无工具调用）；因 config 缺失 / 无 provider 在**任何工具调用前**便 `markTestSkipped` 的可不 dump（TESTING.md §3.2：dump 仅用于已发生工具调用/文件活动之后的失败路径）。**已实现**：`sidecar/test/browser_test.cpp`（Catch2，注册/派发/降级）——探针结构、降级契约（无 stack 时 ops 返回 `ok:false`+error，绝不成功占位）、一次真实持久会话（navigate→snapshot→click 状态延续，验证 C++ 持久进程 spawn/write/read 往返）；每条断言前 `INFO` 打印实际工具调用与 result（C++ 无 debugPrint，INFO 等价日志；sidecar.log 的 `browser-record:` 行即工具调用记录）。3 断言组 11 assertion 通过。
- [x] 5.3 live（gated on 1.1 + 1.2 + 1.3 + 1.4 + 1.5 + 1.6，均已实测回填）：置于 `integration_test/` 且 `@Tags(['live'])` + `library;`；真实模型 + 真 Edge 多步。**断言前** dump 该轮实际浏览器工具调用（toolName、完整 input、result/status）与快照长度；**每条失败路径**（超时 / 错误状态 / 窗口关闭 / **无最终回复(silent-completion)**）在 `fail`/`markTestSkipped` 之前 dump，确保失败可归因；因 config 缺失 / 无 provider / 无 thinking_effort 在**任何工具调用前**便 `markTestSkipped` 的可不 dump（TESTING.md §3.2）；`[OBS]` 现场打印。接受项可观测（均来自 worker 写出的**机器可读记录**——每一 browser 工具调用写一条，如 sidecar.log `browser-record: {"tool":…, "raise_count":…, "popup_closed":…, "download_denied":…, "permission_denied":…, "tabs":…}`，live 测试从该通道读取并 `[OBS]` 打印；这批计数**不进模型可见的 snapshot 文本**，避免污染 context/见 spec 文本快照）：**浏览器以 headless=False 启动且 browser/page 对象活/连上**（不用户可见断言）+ **snapshot 可由程序读取且返回了非平凡文本** + **工具自身 emit 的 bringToFront == 0**（非 OS 外部 focus 事件）+ **popup/新页关闭计数、download denied、permission denied（notifications 路径）、tab 数==1**（其中依赖 task 1.4 才成立的，由 1.4 回填；**遮挡、权限已由 task 1.5/1.6 实测**——离屏执行可作断言；permission-denied（notifications）可作断言、geolocation 仅未授予、非 denied 路径）。**已实现**：`integration_test/browser_live_test.dart`（`@Tags(['live'])`+`library;`，`_pumpUntilFound`+读 sidecar.log `browser-record:`+`dumpToolCards`+`captureLiveShot`），指令引擎 `browser_navigate` data URL→`browser_snapshot`，断言 `raise_count==0`/`tabs==1`/`snapshot_len>0`/计数不在 snapshot 文本+最终回复含 `LIVE BROWSER`；失败路径 dump（wait done timeout → dumpToolCards+readFinalAssistantReply；`Error:` 前缀 → skip）。**实跑未通过（待查，2026-09-06~07）**：见 `TODO.md`。一次运行 exit 0（脚本断言层未抛错，模型真实调用 `browser_navigate`/`browser_snapshot`，sidecar.log `browser-record` count=2，`raise_count=0`/`tabs=1`/`snapshot_len=29`/`permission_denied=2`/`divergent=false`）但**用户目击真窗口白屏卡死**，不能据此判通过；另一次运行 **skipped**（`browser stack unavailable: browser probe did not run`，probe 30s 超时）；手动 probe 仅 1.5s 正常。**真实桌面端到端可靠性未确认，不入验收通过依据**；此前“实跑通过”判定基于单次 exit 0，已撤回。期间修复 live 测试自身 3 个读取 bug（`_readBrowserRecords` 字节偏移读错区、`setPosition` 异步未 await、后换 `setPositionSync`+`setPosition` 前 seek）——属测试代码缺陷；但**白屏卡死 / probe 超时根因未定**，见 TODO.md，待排查修复后重新验证。

## 6. 诚实性审查

- [x] 6.1 对全套 artifacts + 实现跑 **Workflow 对抗验证**（N≥3 视角怀疑者、多数决 kill、离线、参照本地文档），核对：如实（独立工具、非 `web_search` 替代）、无新增矛盾、无隐瞒/降级/删除真 bug、无越界（未改 `web_search`/`web_fetch`/`chat-ui` 行为、未把未验证外部行为当无条件 SHALL）、测试可观测性满足。发现问题按执行顺序插入修复任务（不倒退已勾选），全部到 `[x]` 后输出诚实性审查报告（已审查轮数、每轮问题数、最终任务状态）。**第 1 轮已执行（2026-09-06）**：审查 12 候选，**确认 9 个问题**，全部修复于下方第 7 组；未复活已勾选任务。

## 7. 诚实性第 1 轮返工（发现即修复，不倒退已勾选）

- [x] 7.1 修复 **run_browser_op 静默重启已死 worker**（r1#1/#4，高）：r1 发现 `run_browser_op` 在 `!worker_running()` 时直接 `start_worker()`，绕过了 `send_command` 的 `dead:true` 分支 → AI 收不到死信号、下条命令静默作用于新会话，隐藏死会话。**修复**：改为 `g_ever_started`/`g_dead_reported` 状态机——首次 command=会话启动(spawn)；已启动但死=只报一次 `dead:true`；AI 重开命令=重新 spawn。加 `browser::reset_session(bool was_started)` 测试钩子 + `browser.h` 声明；`send_command` 死分支也置 `g_dead_reported`。**验证**：`browser_test.cpp` 新增「dead worker surfaces dead:true once, then explicit reopen」用例（15 断言/4 用例全过，EXIT 0）。
- [x] 7.2 修复 **弹窗 handler 误关重建主页面/误计 popup_closed**（r1#2，中）：r1 发现重建主页面 `context.new_page()` 也触发 `on("page")`，因 `self._page` 未先更新而把新主页面当弹窗关掉 + 误计。**修复**：handler 只关**有 opener 的真弹窗**（window.open/target=_blank 的 popup opener 非空；程序化 new_page() opener 为 None——已实测验证）。**验证**：worker Dart 测试新增「popup/new-tab is closed」用例（导航 window.open('about:blank') 页 → 记录 `popup_closed>0` 且 `tabs==1`）；stay-hidden 用例非 popup 页记录 `popup_closed=0`。
- [x] 7.3 修复 **click/type 自身导航误判为用户接管**（r1#3，低）：r1 发现只有 `_handle_navigate` 更新 `_expected_url`，click/type 后若页面导航（点链接/提交表单）会被 `_divergence_note` 误报「用户可能导航走了」。**修复**：click/type 成功后 `self._expected_url = page.url`。
- [x] 7.4 修正 **spec/design 过度声称的「显式 deny-downloads/permission-denial 配置」**（r1#5，中）：r1 发现 spec SHALL / design:「deny 下载/权限弹窗（显式配置）」与实际不符（权限/弹窗靠 Playwright 默认 auto-deny/auto-dismiss、下载用 accept_downloads+cancel）。**修复**：spec.md 该 SHALL 与 design.md:40 改为如实描述真实机制（显式 popup 关闭 handler + 下载 cancel + Playwright 默认自动拒权/auto-dismiss；不授予；按步读 `navigator.permissions` 观测）。
- [x] 7.5 修正 **tasks.md:5 / spec.md:6 过期 lib/main.dart 行号**（r1#6，低）：r1 发现本改动使原引用行号（`while(true)`@922 等）漂移。**修复**：按构造描述（while(true) 循环/_executeTool/tool_result 回喂/50-turn cap），注明 apply 时实测当前行号（会随后续改动漂移，故以构造为准）。
- [x] 7.6 修正 **worker 测试断言前未 dump**（r1#7，中）：r1 发现 `browser_worker_test.dart` 对 `browser-record` 接受项 `expect(...)` 在任何 dump 之前，空记录时不可归因。**修复**：断言前先 `[OBS]` dump 全部收到的 browser-record 记录（`browser-record lines received: N` + 每条）。
- [x] 7.7 限定 **live 测试记录到本次运行**（r1#8，中）：r1 发现 `browser_live_test.dart` 从共享 append-only sidecar.log 读记录，旧记录可在本次失败路径上误满足验收。**修复**：测试体发送前记录 `logStart = _sidecarLogPos()`，`_readBrowserRecords(from: logStart)` 只读本次追加的部分。
- [x] 7.8 **raise_count 加可执行的 stay-hidden 回归守卫**（r1#9，低；r2#2 修正）：r1 发现 `raise_count` 恒 0（无任何写入）、`raise_count==0` 断言空洞。**初修**加 `_bring_to_front` 守卫被 r2#2 判为死代码（无调用点，直接 `page.bring_to_front()` 会绕过）。**终修**：删除该死方法；worker 明确从不调用 `bring_to_front`（构造保证），并把**可执行守卫放测试层**——`browser_worker_test` 新增「worker source has no bring_to_front call」用例（扫描 `scripts/browser_worker.py`，断言不含 `.bring_to_front(`，未来任何注入 raise 都会使该测试失败）；记录 `raise_count==0` 如实上报。

## 8. 诚实性第 2 轮（回归 + 新问题）

- [x] 8.1 对全套 artifacts + 实现（含第 1 轮 9 项修复）跑 **Workflow 对抗验证**（N≥3 视角怀疑者、多数决 kill、离线、参照本地文档），**输入携带第 1 轮问题清单及修复状态**，对抗验证者须逐一核对这些修复仍在当前代码成立（未被本轮修复破坏，无回归——见诚实循环规则 8）；并查本轮新增问题。**第 2 轮已执行**：审查 7 候选，**确认 7 个问题**（含 R1 部分修复不彻底 + 新问题），全部修复于第 9 组。

## 9. 诚实性第 2 轮返工

- [x] 9.1 spec.md:39 残留「deny-downloads/permission-denial config / config auto-denies」过度声称 → 改为 Playwright 默认 auto-dismiss/auto-denial + 下载 cancel（r2#1）。
- [x] 9.2 worker 的 `_bring_to_front` 守卫为死代码（r2#2）→ 删除；stay-hidden 守卫改测试层源码扫描（browser_worker_test 新增「worker source has no bring_to_front call」用例）。
- [x] 9.3 worker 测试「counters 在模型可见文本外」是重言式（查 stderr 记录无 snapshot 键）→ 改为断言 worker stdout **响应**不含计数字段（r2#3）。
- [x] 9.4 main.dart `_initSearchAndTools` 变 async 使 `late final _toolDefs` 可能未初始化 `LateInitializationError`（r2#4）→ `_toolDefs` 改非 late 可变，`_initSearchAndTools` 在 await 前同步 `_toolDefs = base`，浏览器/搜索工具后经引用 mutate（无崩溃；gate 测试仍通过）。
- [x] 9.5 popup 关闭修复未回归测试（r2#5）→ worker 测试新增「popup/new-tab is closed」用例（window.open → popup_closed>0 + tabs==1）。
- [x] 9.6 URL/内容分叉检测：spec/design/tasks 声称内容哈希但只比 URL（r2#6）→ worker 新增 `_content_hash`(sha256)+`_mark_expected`(url+hash)，`_divergence_note` 双比较 URL + 内容哈希。
- [x] 9.7 mid-command worker 死亡双报 dead:true（r2#7）→ `send_command` write-fail/timeout 死分支也置 `g_dead_reported`。

## 10. 诚实性第 3 轮（收尾审查）

- [x] 10.1 对全套 artifacts + 实现（含第 1、2 轮全部修复）跑 **Workflow 对抗验证**（N≥3 视角怀疑者、多数决 kill、离线、参照本地文档），输入携带历轮问题清单+修复状态做回归核对，并查新问题。**第 3 轮（收尾）已执行**：审查 6 候选，**确认 5 个问题**，全部修复于第 11 组（达 3 轮上限，收尾轮发现的问题全部修复）。

## 11. 诚实性第 3 轮返工

- [x] 11.1 design.md:46 过期 lib/main.dart 行号（同 R1#6 类未覆盖）→ 改构造描述（r3#1）。
- [x] 11.2 live 测试 `navRecord.containsKey('snapshot')` 重言式断言 → 改为断言记录承载 `raise_count`（计数通道完好；「计数不进模型文本」由 worker 级响应检查覆盖）（r3#2）。
- [x] 11.3 download/permission 拒权无正向回归断言（r3#3）→ worker 测试新增「download is denied」用例（`<a download>` 点击 → `download_denied>0`）；权限拒权需真实 HTTPS 安全上下文、离线无法确定性触发，由 spike 1.6 验证 + worker 从不 grant（记录如实上报）。
- [x] 11.4 design.md:50 过期行号（同 R1#6 未覆盖）→ 改构造描述（r3#4）。
- [x] 11.5 `reset_session(bool)` 声明无条件但仅 `#ifdef _WIN32` 定义，非 Windows 测试链接失败 → 非 Windows 分支补 no-op 存根（r3#5）。

## 12. 诚实性第 4 轮（2026-09-07 用户驱动真实运行发现的新问题；已 round-1 对抗验证，确认 4 处我方 12.x 返工任务的问题，本节据此改写）

> 这几项是 2026-09-07 用户明确质疑 live 测试"糊弄"后，用真实桌面运行 + 全屏采样 + 多轮连跑**实测**发现的。round 1-3 的离线审查（section 6/8/10）看不到"App 白屏冻结 / 探针真的超时 / live 测试只测空页"这类运行态行为；故作为第 4 轮新增返工任务（不倒退 2.4 / 5.3 的勾选）。
> **本轮对抗验证（4 confirmed / 8 killed）已响应**：①F1「**窗口可见性不是 bug**——spec 的 Stay-hidden 只要求 SHALL NOT 每步 bring-to-front（已满足 + source-scan 守卫），且 spec.md:35 明确允许 `operate user-visibly`，工具本就**用户可见**」→ 用户已拍板：**保持用户可见、只改 App 冻结**。已删除原先 12.1「窗口未隐藏/隐藏窗口」的错误主体，改为诊断+修复 App 冻结；②r4-3「12.x 缺 App 白屏冻结任务」→ 新增 12.1（诊断+修复 App 冻结，**真 bug**，是 12.3 live 验收的前置）；③F5「12.4 缺规则 8 回归携带」→ 12.4 明确携带 rounds 1-3 清单+修复状态；④F3「12.1 误引 1.5 证据」→ 已删除误导性引用。被 kill 但仍采纳：12.2 的「复用持久 worker」**不可行**（探针在 init 跑、会话未建）→ 已更正；12.3 的 Bing 结果新 tab 冲突 → 改为 **resolve-first 前置**；12.3 rewrite 须 **preserve 已修 fixes**。

- [ ] 12.1 **诊断 + 修复 App 白屏冻结（用户实锤：alias_agent 窗口整体变白、无文字并冻死）**（覆盖缺口 r4-3，**真 bug**；**不是**"窗口可见性"——用户已拍板保持 user-visible，spec 允许，非缺陷）。
  **证据**：用户目击 alias_agent 窗口整体变白、无任何文字并冻结（另一实时运行未复现——**间歇性**）。TODO.md「白屏卡死 + probe 超时根因未定」列候选：headed Edge worker 真实桌面渲染/资源不稳、`_initSearchAndTools` async 化（await browserAvailable 影响启动）、integration_test `-d windows` 真窗口渲染。**注意**：全屏采样确认过「Edge 窗口弹出盖住 app」（frame_1788711800）——但那是 spec 允许的用户可见，**不是**本任务；本任务只针对 **App 自身冻结**，两者不可混同、不可用"Edge 盖住"当 App 冻结的结论。
  **修复方向**：先**复现 + 定位真因**（不预设）。用 CopyFromScreen 全屏采样连跑 live（≥5 次），观察 App 窗口是否出现"纯白/无文字/不刷新"帧；同时核对 `_initSearchAndTools`（lib/main.dart:330-673，await browserAvailable@518）、`browser_available()`（sidecar/src/browser.cpp:467，一次性 subprocess probe `subprocess::run` @507）、headed Edge worker 生命周期。区分：(a) Dart 主 isolate 被同步 FFI/await 卡住；(b) Flutter raster/渲染线程卡住；(c) headed Edge 渲染把桌面/合成器拖垮。定位后按因修：若是 init 探针拖慢/阻塞 → 结合 12.2 降本/异步化；若是 headed Edge 渲染竞争 → 换渲染/启动参数；若是某已修 fix 引入的时序 → 回改。
  **本轮诊断 + 复现结果（2026-09-07 实跑 2 次）**：**2 次 live 实跑 app 全程正常**（工具卡片、流式回复都渲染，2:56 / 4:22 跑完，无白屏冻结、无刷新停摆、输入/上下文可交互）。**本轮未复现**。**静态分析排除 (a)**：`browserAvailable`/`browser_*` ops 都经 `Isolate.spawn(_webWorkerMain)`（sidecar_bridge.dart:654-694）到独立 isolate，FFI `subprocess::run` 在空闲 isolate 跑，**主 UI isolate 不被同步 FFI 阻塞**。故 (a) 排除；(b)/(c) 属 headed Edge GPU/raster 竞争或 harness 工件（间质性，未复现）。**诚实边界（未假定已修复）**：复现不了（间歇性）则如实报"本轮未复现 + 已加观测待再触发"，**不得假定已修复**——本项仍为 12.4 live 验收前提（r4-3），一旦复现须定位真因再修。
  **验证**：用 CopyFromScreen 全屏采样连跑 live ≥5 次（跑 `flutter test --tags live --run-skipped integration_test/browser_live_test.dart -d windows` 只为把 app 真窗口拉起做观察），**断言 App 窗口帧始终含真实内容（标题/气泡/卡片，非纯白无文字），且 App 自身上下文/输入框持续可交互、无刷新停摆**。本项**只验 App 冻结**：live 测试整体"通过"留到 12.4（依赖 12.2 先修好探针、12.3 跟进标签页、12.4 重写测试，均在 12.1 之后，故 12.1 不以 `flutter test` 通过为验收，避免向前依赖）。**诚实边界**：复现不了（间歇性）则如实报「本轮未复现 + 已加观测待再触发」，**不得假定已修复**；本项是 12.4 live 验收的前置（否则 12.4 的"通过"被同一冻结卡住——r4-3）。
  **观测复现结果（2026-09-11，修好观测器后的 1 轮 live 实跑）**：**未观测到 App 冻结**——237 帧 / 约 4.5 分钟全程 `IsHungAppWindow` 零次为真。**但完整复现了「窗口整体变白、无文字」的观感**，来源是 **headed Edge 窗口盖在 app 的窗口矩形上**：s61-s64（约 4s）Edge 停在 `about:blank` → 纯白、零文字（white=83.2% / colored=0%）；s268-s272 Edge 停在 Bing 结果页 → 大面积白（white=76.5%）。覆盖度量化：**237 帧中仅 25 帧（10.5%）能完整看到 app**，203 帧（85.7%）被 Edge 部分遮挡。同轮 live 测试本身因模型把回合耗在选择器超时上、未产出最终回复而失败（`a final assistant reply must exist`，exit 1）——**该失败与冻结无关，属模型/网络 flakiness**，不构成本项证据。
  **观测方法（可复用，避免重踩）**：脚本 `%TEMP%\aliasagent-window-observer.ps1` —— 用 `EnumWindows` 按 **PID + 窗口类 `FLUTTER_RUNNER_WIN32_WINDOW`** 定位窗口（**禁止用 `FindWindow($null,'alias_agent')`：PowerShell 会把 `$null` 编组成空字符串而非 NULL，导致永远匹配不到——首版观测器因此截到 0 帧、空跑一轮**），再用 `CopyFromScreen` **只截 app 窗口矩形**（不截整屏），记录近白像素占比 / 彩色像素占比 / `IsHungAppWindow`。基线（正常空闲 app）：`white=0% / colored=5.1% / hung=False`，首帧约 2.0s。另注：`flutter test integration_test/... -d windows` 产出的 `build\...\Debug\alias_agent.exe` 入口是**测试文件**，直接启动会一直等驱动器、永不显示窗口——**不可拿该 exe 直接启动来判定「app 起不来」**（本次已排除该假线索）。
  **诚实边界（本项仍未结案）**：以上**只证明「本轮未冻结」+「白屏观感可由 Edge `about:blank` 完整解释」**，**不证明 App 冻结不存在**（间歇性，仅 1 轮观测）。且须守本 task 既有约束——**不得用「Edge 盖住」直接冒充 App 冻结的结论**；真因归属仍需复现，或补足观测后再判。

- [x] 12.2 **修复 `browser_available()` 探针稳定性——一次性 subprocess probe 在真实桌面负载下 30s 超时**（新发现，已复现）。**已修复 + 已验证（2026-09-07）**。
  **证据**：TODO.md + tasks 5.3——手动 `python browser_worker.py {"cmd":"available"}` 仅 ~1.5s 返回 available:true，但 C++ `subprocess::run`（30s，browser.cpp:467 `browser_available()` 内 @507）在真实桌面负载下超时 → live 运行 skipped `browser probe did not run`。根因方向：每次探针都重新 spawn 一个一次性 python + headless Edge 冷启动，系统负载下不稳。
  **修复方向**（**不可**"复用持久 worker"——探针在 app init 跑（lib/main.dart:518，_initSearchAndTools@initState:303），此时无任何浏览器会话；持久 worker 是**每次 browser 任务才懒启动**（browser.cpp `run_browser_op`），探针时刻无可复用对象，killed F2 指出该方向不适用，据此去掉）：降探针成本（减少一次性 headless Edge 冷启动）/ 加长超时 + 重试 / 缓存探针结果避免重复冷启动；或让探针不阻塞声明（异步声明、失败降级不声明）。目标：live 不因探针超时被 skip。
  **已实现**：`browser.cpp` `AVAILABLE_TIMEOUT_SEC` 30→60 + `AVAILABLE_PROBE_ATTEMPTS=2`（重试一次）；`browser_worker.py` `_probe_channel` 加 `timeout=PROBE_LAUNCH_TIMEOUT_SEC*1000`（25s）+ `_probe_available` 加 `PROBE_ATTEMPTS=2`（骑过瞬时负载）；`sidecar_bridge.dart` `browserAvailable` 隔离等待 60→120s。
  **诚实更正（round-1 对抗验证 sec12/F2 confirmed，已修复）**：初版把 `available:false` 也缓存为权威结果——worker 的 20s 有界启动超时把"慢冷启动（瞬时负载）"转成 `available:false`，C++ 又把它缓存为"真缺席"且不重试 → 整个 run 低估工具 → 重新引入 "live skipped"。**修复**：`browser.cpp` **只缓存能证实成功的 `available:true`**，`available:false` **不缓存、并在调用内重试**（`continue`）——瞬时负载不再被持久化；`PROBE_LAUNCH_TIMEOUT_SEC` 20→25 + `PROBE_ATTEMPTS=2` 让慢启动被骑过去。降级语义（真缺席→不声明）保持不变。
  **验证**：sidecar_tests `[browser]` 15 断言全过 + worker 层 `browser_worker_test` 5 测试全过；**2 次 live 实跑探针均过 gate（`available:true`，未 30s 超时）**。**诚实边界**：探针超时仍是运行不稳现象（非功能缺陷）；不改降级语义。

- [x] 12.3 **实现「跟进新标签页」= 设计 甲（F4 设计决策，2026-09-08 实测确认）——点击开 `target=_blank` 标签页时工具跟随它（不关），让 AI 读到真实目标页**（**是下方 12.4「点结果→读落地页」验收的前置**）。
  **背景 / 证据（2026-09-08 真必应探针，raw Playwright + `expect_page`）**：点击必应第一条结果（`li.b_algo h2 a`，`href=https://flutter.cn/`、`target=_blank`）→ **确实新开标签页**（`expect_page` 捕获，加载后读到 flutter.cn 真实标题+正文）。此前「单标签页」关闭 handler 把该新页当弹窗**立即关掉**（即用户看到「一点开新标签页就关浏览器」）→ AI 永远读不到目标页。另：不处理权限框时必应会弹 `edge://permission-request-dialog/` 阻塞导航；`grant_permissions([])`（显式拒权）可消除。
  **修复（worker `scripts/browser_worker.py`，**已实现（2026-09-08））**：`_handle_click` 用 **`context.expect_page(timeout=NEW_PAGE_WAIT_SEC)`** 在点击后**捕获并跟随新标签页**（新页成为 `self._page`、计入 `_adopted_tabs`；不是短轮询 `ctx.pages`——实测 poll `ctx.pages` 在新页打开后**读不到**（popup 事件只在 Playwright API pump 时 flush），故用 expect_page 这个可靠原语）；**等新页加载**（`_wait_for_page_ready`=`wait_for_load_state('domcontentloaded')`）；`on_page` **不再自动关窗**（只记 `_browser_opened`，作可观测）；`new_context` 后 **`grant_permissions([])` 拒权**；新增 `_browser_opened`/`_adopted_tabs` 计数。**NEW_PAGE_WAIT_SEC 实测调为 1.0s**：目标页打开的 Page 事件实测 ~0.16–0.36s 注册（事件先于内容加载，内容由 `_wait_for_page_ready` 处理），1.0s 是 ~2–6× 余量；**in-place（不新开页）点击因 expect_page 阻塞会多付该窗口等待——实测从 3.0s 降到 ~1.06s**（真 bug 权衡，已记录）。若给 AI「切回搜索结果页」：加 `browser_back`（切到上一个页）或接受重导航（用户选甲已接受「能切回」）。
  **实测验证（2026-09-08，worker 层端到端 + C++ sidecar）**：真 local HTTP 两页 fixture（`/` 结果页 `<a target=_blank href=/landing>`）用**真 worker** 驱动——`click #r` → `snapshot` **返回 `LANDING`**、`url=/landing`、record `browser_opened=1`/`adopted_tabs=1`/`tabs=2`（✓）`raise_count=0`；**in-place** fixture（按钮 `location.href='/done'`）`click #go` → snapshot `DONE`、url=`/done`（✓ 无虚假跟随）。`test/integration/browser_worker_test.dart` 5 测试全过；`flutter analyze` 该文件无新警告；`sidecar_tests.exe [browser]` 10 断言 4 用例过（真实会话段经 worker 层端到端印证）。**诚实 caveat（数据 URI 教训）**：初版测试用 `data:text/html` 作 `target=_blank` href → Chromium **不把新 tab 导航到 data: URL**（停留 `about:blank`），无法测跟随后快照；改用真 local HTTP 服务器后成立——这是测试 fixture 修正，非放宽验收。
  **spec 同步（stay-hidden 单 tab → 甲）**：把「单 tab 复用 + 显式 popup 关闭」改为「**跟进点击目标页（不关），AI 通过 back/会话关闭管理多标签页**」——用户拍板的设计决策（甲）。注意：真·弹窗（网页自弹广告）也**不再自动关**（甲「保留多标签页」，会话关闭兜底），与原「单 tab 抑制弹窗」spec 不同，由选甲背书。**spec/design 已同步**；并已把「stay-hidden 要求 + 两个场景」改写为 follow-new-tab 语义，同时如实补了 **grant_permissions([]) 使 geolocation 也 'denied'**（严格于 task-1.6 的 'prompt'）这一可观测变化（在 spec 中注明来源为 worker record，非 task-1.6 spike——两者界限分明，不混同）。
  **验证（worker 可自测 + live；**须同步更新既有"单 tab"断言**——甲 是多标签页设计，`tabs>1` 是预期，不是 bug）**：
  - `test/integration/browser_worker_test.dart`「popup/new-tab is closed (single-tab reuse)」用例**重写**为「点击 `target=_blank` 链接 → worker **跟随新页**（`snapshot` 返回新页内容，非原页）、`tabs` 如实上报（可>1）」——原断言 `popup_closed>0`/`tabs==1` 与甲 冲突；**已重写 + 实测**。
  - `integration_test/browser_live_test.dart` `_expectStayHidden` 的 `tabs==1` 改为**每步 `raise_count==0` + `tabs` 如实上报**（甲 下可>1）；同时`browser_worker_test.dart`「worker source has no bring_to_front」仍在；**`_expectStayHidden` 已改**（只断 `raise_count==0`，tabs 如实 `[OBS]` 打印）。
  - `browser_worker_test.dart` 新增用例——本地 fixture 页含 `<a target=_blank>` 链接，`click` 后 worker **跟随新页、`snapshot` 返回新页内容（非原页）**；**已新增（local HTTP fixture）+ 实测**。
  - spec.md / design.md 的「单 tab 复用 + 显式 popup 关闭」改为「**跟进点击目标页（不关），AI 通过 back/会话关闭管理多标签页**」（与 12.3 修复段一致）；**已同步**。
  - live 真必应：模型 `click` 第一条结果 → 读到真实结果页标题/内容（达成 12.4「读落地页」）。**本项属 12.4**（依赖「跟进」先落地），由 12.4 的实跑验证——不在 12.3 单测内。
  **诚实边界**：「跟随」= 点开的新标签页变成当前读的页；AI 要回结果页需 `back` 或重导航；不再自动关弹窗是与原 spec 的**设计变更**（用户已选甲认可）。

- [x] 12.4 **重写 live 测试为真实 Bing 搜索多步**（新发现，5.3 现状「糊弄」——只让模型开 data: 空页读标题，测不到点击/输入/内容识别/多步）。
  **证据**：`browser_live_test.dart` 现在只 prompt 模型 `navigate` 一个 `data:text/html,<h1>LIVE BROWSER</h1><p>alpha text here</p>` 空页 + `browser_snapshot`，断言 `snapshot_len>0` + 回复含 "LIVE BROWSER"；不测 type/click/内容识别/多步。worker 层（`browser_worker_test.dart`）虽测了 click/type/popup/download，但绕过真模型 + 真 DLL + 真 App——「真模型能否驱动真实多步浏览」这条真价值主张**两头都没验证**（design Open Q4：快照 format（innerText vs accessibility）在此被实测）。
  **必须保留已修（防回归，killed F3）**：本文件已含 rounds 1-3 修复——7.7/R1#8 `logStart` 仅读本次、`setPositionSync`+seek、`Error:` 前缀 skip。**重写必须保留**这些已修行为。
  **resolve-first 前置（killed F4）**：Bing 结果常 `target=_blank` 新开页，与「单 tab 关所有新页」handler 冲突 → 若点开结果被 handler 关掉，12.4 的「click 第一条结果 → snapshot 落地页」验收**不可达**。**先实测**「结果新页该关还是该留」，据结论改 handler（允许主流程/结果页不被关，仅关真弹窗）或改验收——这是**前置决策**，不得一句话"必须实测"带过。
  **验收**：live 测试改用真实 Bing 搜索（接受外网依赖）：真模型 + 真 Edge 完成「`navigate` bing.com → `type` 独特关键词进搜索框 → 触发搜索 → `snapshot` 结果 → `click` 第一条结果 → `snapshot` 落地页 → 报告标题+内容」。断言基于 browser-record 通道（真实步骤）抗非确定性：① 关键词被用到——**type record input 含独特关键词，或 navigate 到含关键词的 `?q=` URL**（Bing/cn.bing 会重定向，模型可能直连搜索 URL 而不显式浏览器 type；两法都证明"模型确实把这个关键词送进搜索"，二选一成立即可，见下方实测结果）；② click 前后 `snapshot_len` **不同**（通道携带的长度信号；快照正文在模型侧 content，不进 record 通道）→ 发生导航/DOM 变化；③ **内容识别交叉验证**——模型「声称」的结果标题/URL 必须**真的出现在某条 browser-record 的 `title`/`url` 里**（通道携带元数据；证明真看到、非幻觉）；④ `raise_count==0` 每步后成立（工具自身从不 bring-to-front）；`tabs` 如实上报——甲 多标签页设计下点击结果会 `tabs>1`（跟随的新页/真弹窗不关），**不强制==1**（见 12.3 同步）。
  **Open Q4 诚实边界（killed 12.3-openq4）**：「仅 innerText 快照能否定位元素选择器/识别内容」是**待实测记录**的开放问题——验收**不得预先假定成功**。模型若无法从文本快照给出 `input[name=q]` 等选择器 → 记为**真实结论**（"文本快照不足以定位元素，需快照附可定位信息/accessibility"），另立子 change 处理，**不等于测试 bug、不得静默降级、不得写成已确认能力**。模型跳步/选择器失败 → 如实记"模型未能完成交互/定位"（诚实 fail 非 bug）；Bing 反爬（`navigator.webdriver` → 可能需 `--disable-blink-features=AutomationControlled`）、中国区端点（cn.bing vs bing）、网络 flake → 如实报，不静默改断言。
  **已重写（2026-09-07）**：`browser_live_test.dart` 改为真实 Bing 多步（navigate bing → type "Flutter" → 触发搜索 → snapshot 结果 → click 首条 → snapshot 落地 → 报告标题/内容/URL）。**round-1 对抗验证发现 F1（我的测试 bug）已修复**：初版把快照正文当成 browser-record 通道的字段——但该通道按 spec.md:24 只携带 `snapshot_len`/`url`/`title`，不携带快照正文 → 初版断言永远失败。**修复**：改为用记录携带的 `snapshot_len`（验收 ②：click 前后快照长度变化）与 record 的 `title`/`url`（验收 ③：内容识别交叉验证）。app 侧 `_executeTool` 仍把快照正文放进模型可见 content（但那是模型侧，非 record 通道）。
  **实跑结果（2 次，2026-09-07）**：模型确实**真多步浏览**了（navigate bing→cn.bing、type "Flutter" 进 #sb_form_q、navigate ?q=Flutter 搜结果、多次 click 结果链接）。**接受项部分未过**：①（关键词，type 或 navigate-?q）与②（`snapshot_len` 变化）过；**③（标题/URL 交叉验证）与④（原 `tabs==1`）未过**——最终回复缺失（模型被 F4 卡住没产出文字）+ `tabs` 2/4、`popup_closed` 到 3（单 tab 在真实 Bing 上不成立）。注：③ 的最终回复缺失 + ④ 的 `tabs==1` 不可满足，正是 12.3 甲 要治的（跟随新页→模型能读到落地页并产出回复；`tabs>1` 改为如实上报）。
  **实跑暴露的真实产品/设计级发现（如实记录，非测试 bug、非放宽验收）**：
  1. **F4 实锤**：点击 Bing 结果（`li.b_algo h2 a`/`#b_results h2 a`）→ **新开 tab**（`popup_closed` 递增 1→2→3），single-tab 关闭 handler 把它关掉 → **模型读不到落地页**，只能绕道 `navigate https://flutter.dev`。
  2. **Open Q4 实锤**：模型猜 `#sb_form_go`/`#search_icon`/`button[type=submit]`/`a[href="..."]` 全**超时不存在**（cn.bing.com 上无这些选择器，文本快照也不含选择器）→ 模型无法从 innerText 可靠定位元素。
  3. **single-tab tabs>1**：真实 Bing 交互下 `tabs` 2/4（noopener 新页未被 handler 关闭、或异步关闭竞争）→ 单 tab 复用保证未真正成立。
  **结论（如实，未打勾）**：12.4 的**重写本身完成**（真实多步 + 诚实断言 + 保留 rounds 1-3 修复：logStart 本次限定 / setPositionSync+seek / Error: skip / captureLiveShot）。但**验收未通过**——实跑暴露的 F4（single-tab 关结果页→读不到落地）/ Open Q4（无选择器）/ single-tab tabs>1 是**真实产品/设计级问题**，其中 F4 与 single-tab 涉及 spec 的 stay-hidden 单 tab 语义与"读落地页"目标的**设计冲突**（需设计决策），Open Q4 需另立子 change（快照附可定位信息/accessibility）。**不得放宽验收掩盖、不得静默降级、不得写成已确认能力**。
  **终跑（2026-09-08，12.3 甲落定后）**：`flutter test --tags live --run-skipped integration_test/browser_live_test.dart -d windows` → **`+1 All tests passed`**（01:20）。真实 Bing 多步：navigate bing.com → type "Flutter"（`#sb_form_q`）→ click `#search_icon` 触发搜索 → snapshot 结果 → **click `#b_results h2 a` → follow 新 tab**（record `browser_opened=1`/`adopted_tabs=1`/`tabs=2`，url=`https://flutter.dev/`、title="Flutter - Build apps for any screen"）→ snapshot 落地。**验收全过**：① 关键词（type input 含 "Flutter"）✓；② click 前后 `snapshot_len` 变（`3542`≠前值）✓；③ **内容识别交叉验证 `anyTokenInRecordTitleOrUrl=true`**（模型回复的 "Flutter - Build apps for any screen" / `https://flutter.dev/` 真出现在某 record title/url——非幻觉）✓；④ `raise_count==0` 每步成立 + `tabs` 如实（结果点击后 `tabs=2`，甲 允许）✓。**逐项对应**：③ 之前失败正是 12.3 甲 治的（single-tab 关结果页 → 模型读不到落地 → 无最终回复）；④ 之前 `tabs==1` 不可满足 → 改如实上报。**Open Q4 实测已回答**：模型从 innerText 快照**能**给出有效选择器（`#sb_form_q`/`#search_icon`/`#b_results h2 a`），仅 `#sb_form_go`（submit，`aria-hidden="true"`）不可点——模型注明原因并改用 `#search_icon`，是**真实交互适配、非失败降级**，如实记录。**12.1 App 冻结**本次 live 全程未见（1 次运行 app 正常、无白屏冻结）——仍属"未复现"（见 12.1 诚实边界），12.4 打勾不代表 12.1 已修复。**诚实声明**：终跑通过依赖真实外网 Bing + 真实模型；Bing 页面/反爬变动可能让后续运行 flaky，如实报不静默改断言。

- [x] 12.5 **诚实性审查（Workflow 对抗验证）**：对 12.1-12.4 新增返工任务跑 **Workflow 对抗验证**（N≥3 视角怀疑者、多数决 kill、离线、参照本地 docs+代码）。**必须携带历轮问题清单 + 修复状态做回归核对（CLAUDE.md rule 8，F5）**：将 rounds 1-3 已确认问题及其修复状态（7.1-7.8 / 9.1-9.7 / 11.1-11.5）+ 本轮 F1 / r4-3 / F5 / F3 修复状态作为 Workflow 输入，让对抗 agent 逐一验证这些修复在当前代码**仍成立**（未被 12.1-12.4 破坏），发现回归 → 追加修复任务。并核对：12.1-12.4 各处发现是否真实（查 `sidecar/src/browser.cpp` / `scripts/browser_worker.py` / `integration_test/browser_live_test.dart` / `lib/main.dart` / `docs/*.md` / `TODO.md`，亲自 Read 核到）、任务是否 proposal 级可执行/可自测/有序、是否弱化验收、是否过度声称（12.4 OpenQ4 为**待实测**非已确认 bug；12.1 不得用"Edge 盖住"充当 App 冻结结论）、12.5 是否为最后一项。发现问题按执行顺序插入修复任务，全部 `[x]` 后输出诚实性审查报告（已审查轮数、每轮问题数、最终任务状态）。
  **round-1 审查已执行（2026-09-07，review_r4_v4.mjs）**：5 候选，**确认 3**（F1：12.4 测试 ③④ 读了通道不存在的 snapshot 文本——已修复；F2/sec12：12.2 探针缓存把瞬时负载当权威缺席并持久化——已修复；均已在上方 12.2/12.4 记录），kill 2。
  **round-2 回归携带（review_r4_v5.mjs）**：5 候选，**确认 1**（r2-f1：12.4 验收 ③ 文案仍写"出现在 browser-record 的 snapshot 里"，与修正后做法矛盾——已修复验收 ② 用 snapshot_len/③ 用 title-url），kill 3。
  **round-3 收尾（review_r4_v6.mjs）**：5 候选，**确认 2（均 LOW）**（sec12-1：验收 ① 写"type 含关键词"但测试是 type-or-navigate-?q——已改为二选一；sec12-stale-browser-cpp-line：12.1/12.2 引 browser.cpp:450 但 probe 实为 :467/@507——已改），kill 3。
  **round-4 最终验证（review_r4_v7.mjs）**：**0 候选，clean**（3 维度 agent 各自 Read 后返 `{"findings":[]}`，共 43 工具调用，非空转）。
  **round-5 审查（2026-09-08，review_r5_v1.mjs，对 12.1-12.4 落定后 + 回归携带 rounds 1-4）**：4 候选，**确认 3（均 LOW）**，kill 1——①`browser.h:17` 仍有"closes popups/new tabs so the session stays single-tab"旧注释（与甲 冲突，3/3 确认）；②`design.md` OQ6 仍写"deny-downloads/permission-denial 配置"（9.1 已消除的过度声称残留，2/3 确认）；③`browser_live_test` 验收 ④ anyToken 交叉验证被关键词 "Flutter" 饱和（instructed + 见于搜索记录 title/url → 无法证明非幻觉，3/3 确认）。kill：spec"AI manages multi-tab via session"未由 tab 命令支撑——被 3/3 refute（限制已在 tasks 12.3 诚实记录 + 用户选甲接受重导航）。**3 项确认 → 追加为新任务 12.6/12.7/12.8（按执行顺序），修复后跑 12.9 再审查（最后一项）**。
  **审查结论**：**12.1 / 12.4 现为 `[x]`**（12.4 终跑通过，见上）；12.1 仍为 `[ ]`（间歇未复现）。本项（round-5）发现 3 处 LOW 待修复，按诚实循环追加为 12.6-12.8 并回退审查。

- [x] 12.6 **修正 `sidecar/src/browser.h` 的过期「单 tab 关弹窗」注释**（round-5 确认 #1，LOW；**文档注释不一致，非行为 bug**——实际 worker 已是「跟进不关闭」）。`browser.h:15-19` 头注释仍写"closes popups/new tabs so the session stays single-tab"，与 12.3 甲（跟进不关、多 tab 预期）直接矛盾，会误导读该公共 API 注释的审查者/实现者。**修复**：把该句改为与 12.3 甲一致的描述——"the worker never closes popups/new tabs; it FOLLOWS a target=_blank / window.open new tab (context.expect_page) and keeps multi-tab; the original tab stays open"。**已修复（2026-09-08）**：grep `browser.h` 确认含 "closes popups"/"stays single-tab"/"single tab" 0 处；已含 "design 甲 (follow new tab)"/"FOLLOWED"/"multi-tab" 描述。**诚实边界**：仅改注释，worker/测试/spec 已是甲，行为未动。

- [x] 12.7 **修正 `design.md` Open Question 6 的「deny-downloads/permission-denial 配置」过度声称**（round-5 确认 #2，LOW；**文档措辞残留在 7.4/9.1 未及处**）。`design.md:71` OQ6 仍写"用…deny-downloads/permission-denial 配置实现"，而实现**无该配置**（以 download.cancel() + Playwright 默认 auto-deny/auto-dismiss + grant_permissions([]) 实现）。7.4/9.1 已在 spec.md:39 / design.md:40 清除该过度声称，OQ6 漏网。**修复**：把该句"deny-downloads/permission-denial 配置"改为如实机制描述（"download cancel + Playwright 默认 auto-deny/auto-dismiss + grant_permissions([]) 前置拒权"）。**已修复（2026-09-08）**：grep `design.md` 不含 "deny-downloads/permission-denial 配置"/"deny_downloads"/"deny_permissions"（仅 tasks.md 含历史记录：原 1.4 spike 记录 + 7.4/12.7 描述的合法历史）。**诚实边界**：只改机制措辞，OQ6 结论（1.4/1.6 实测）+ 已注明的 grant_permissions([]) 保留。

- [x] 12.8 **强化 `browser_live_test.dart` 验收 ④ 的「内容识别交叉验证」，使其不被关键词 "Flutter" 饱和**（round-5 确认 #3，LOW；**验收的防幻觉强度被高估，非行为 bug**）。现实现 `replyTokens.any((t) => seen.contains(t))`，而 "Flutter"（7 字符、`_kKeyword`、instructed）同时见于模型回复与搜索记录的 title/url → 任意回复/标记仅凭该关键词即恒真，**无法证明模型描述了真实访问页**（与注释/12.4 ③声称的"证明真看到、非幻觉"不符）。**修复**：改为**排除关键词的判别式交叉验证**——只对「跟进/落地页」记录（`url` 非 bing 搜索页、`title` 非"搜索"页）的 title/url 取 token，要求模型回复 token **去掉 `_kKeyword` 后**仍有 token 命中（关键：被记数的那条记录必须是点击后跟随的落地页记录，而非搜索结果页记录）；若没有任何落地页记录则如实 fail（不得静默降级）。**已修复 + 实跑验证（2026-09-08）**：live 终跑过（`+1 All tests passed`），`[OBS] content-recognition: landing records=2（均 flutter.dev，bing 搜索页已排除） nonKeywordReplyTokens=51 anyTokenInLandingRecord=true`——④ 现凭**真实落地页 token**（flutter.dev / "Build apps for any screen"）而非关键词命中；仿幻觉 "acme.example" 回复将无法匹配。`flutter analyze` 新版 0 issue。

- [x] 12.9 **诚实性审查（Workflow 对抗验证，round-6）**：对 12.6-12.8 修复跑 **Workflow 对抗验证**（N≥3 视角怀疑者、多数决 kill、离线、参照本地 docs+代码）。**必须携带 rounds 1-5 全部问题的修复状态做回归核对（rule 8）**：逐项验证 7.1-7.8/9.1-9.7/11.1-11.5 + 12.3/12.4 修复在**当前代码仍成立**（未被 12.6-12.8 破坏——尤其 12.8 改验收不得弱化③的防幻觉意图、12.6/12.7 是纯注释/措辞不得影响行为）。并核对：12.6-12.8 各自修复是否真实落地（Read `browser.h`/`design.md`/`browser_live_test.dart` 核到）、是否从根上消除而非换皮、是否未越过本 change 范围/未弱化验收。发现问题按执行顺序插入修复任务，全部 `[x]` 后输出诚实性审查报告（已审查轮数、每轮问题数、最终任务状态）。
  **round-6 审查已执行（2026-09-08，review_r6_v1.mjs）**：3 候选，**确认 2（均 LOW）**——①`design.md:55`（Risk 5）仍写"以 deny-downloads/permission-denial 机制压制"（同 7.4/9.1/12.7 已清的过度声称残留，用"机制"非"配置"故 12.7 grep 漏网，2/3 确认）；②`tasks.md:124` 12.5 勾 `[ ]` 但其 findings 12.6-12.8 已 `[x]`，与 6.1/8.1/10.1（findings 解决后即 `[x]`）约定不符（2/3 确认）。kill 1（①的"wrong-mechanism"严重版——被 3/3 refute：`机制`非`配置`、同 clause 已给出 grant_permissions([])/cancel 真机制、9.1 清除的是"显式配置"不是"机制"）。**2 项确认 → 追加 12.10/12.11 修复；修复后跑 12.12 收尾审查（最后一项，round-7 达 3 轮上限）**。

- [x] 12.10 **修正 `design.md` Risk 5（:55）的「deny-downloads/permission-denial 机制」过度声称残留**（round-6 确认 #1，LOW）。`design.md:55` 仍写"下载/权限弹窗仍以 deny-downloads/permission-denial 机制压制"，同 7.4/9.1/12.7 清理的过度声称——"机制"用词使 12.7 grep "配置"漏网。实现在 worker：`accept_downloads=True`+`_on_download`→`download.cancel()`（下载）+ Playwright 默认 auto-deny/auto-dismiss + `grant_permissions([])`（权限）。**修复**：把"deny-downloads/permission-denial 机制"改为如实机制描述（"download cancel + Playwright 默认 auto-deny/auto-dismiss + grant_permissions([]) 前置拒权"）。**已修复（2026-09-08）**：grep `design.md` 不含 "deny-downloads/permission-denial"（0 处）；:55 现如实写"下载/权限弹窗以**下载 cancel（`accept_downloads`+`_on_download`→`cancel()`）+ Playwright 默认 auto-deny/auto-dismiss + `grant_permissions([])` 前置拒权**压制"。同句 1.4/1.6 实测结论保留。诚实边界：只改压制机制措辞，不动物理逻辑。

- [x] 12.11 **修正 `tasks.md` 12.5 的勾选状态——findings 已解决则 `[x]`**（round-6 确认 #2，LOW；**任务记录与文件自身约定不一致**）。`tasks.md:124` 12.5 勾 `[ ]`，但其 findings 12.6-12.8 均已 `[x]`；文件其它审查任务 6.1/8.1/10.1 均在 findings（7.x/9.x/11.x）解决后勾 `[x]`。**修复**：12.5 勾 `[x]`（保留其叙述性 body 记录 rounds 1-5 审查 + 12.6-12.8 解决）。**已修复（2026-09-08）**：tasks.md 12.5 现为 `- [x]`；6.1/8.1/10.1/12.5 四审查任务勾选约定一致。诚实边界：只改勾选（12.5 的审查确已执行 + findings 12.6-12.8 确已解决，符合 6.1/8.1/10.1 的约定）；不改 12.1 的 `[ ]`（间歇未复现，本就未解决）。

- [x] 12.12 **诚实性审查（Workflow 对抗验证，round-7 收尾）**：对 12.10/12.11 修复 + 全 section-12 回归跑 **Workflow 对抗验证**（N≥3 视角怀疑者、多数决 kill、离线、参照本地 docs+代码）。**必须携带 rounds 1-6 全部问题的修复状态做回归核对（rule 8）**：逐项验证 7.1-7.8/9.1-9.7/11.1-11.5/12.2-12.10 修复在**当前代码仍成立**（未被 12.10/12.11 破坏——纯措辞/勾选不得影响行为或其它修复）。并核对 12.10/12.11 是否真实落地、是否从根上消除、是否未弱化验收/未越界。**此轮为 3 轮上限后的收尾轮**：发现问题按执行顺序插入修复任务并全部修复，然后输出诚实性审查报告（已审查轮数、每轮问题数、最终任务状态）。
  **round-7 审查已执行（2026-09-08，review_r7_v1.mjs）**：**0 候选，clean（confirmed=0 / totalFindings=0）**。3 维度 agent 各自 Read 后返 `{"findings":[]}`（共 64 工具调用，每个文件被 3 个 reviewer 各 Read 一次——browser.h/design.md/spec.md/tasks.md/browser_worker.py/browser_worker_test.dart/browser_live_test.dart 全覆盖，**非空转**）。**审查结论**：12.10/12.11 修复真实落地 + 不破坏任何 rounds 1-6 修复（无回归）；section-12 各处一致，无弱化验收、无越界。本项诚实性审查循环已收敛干净 → 诚实性审查报告在下方给出。

- [ ] 12.13 **修「headed 浏览器窗口盖住 app」——让浏览器与 app 并存可见**（round-8 新发现，源于 12.1 的观测复现；**真 UX 缺陷**，但与 app 冻结是两件事）。
  **证据（2026-09-11，修好观测器后 1 轮 live 实跑，237 帧 / 约 4.5 分钟）**：用 `EnumWindows`（PID + 窗口类 `FLUTTER_RUNNER_WIN32_WINDOW`）**只截 app 窗口矩形**统计 —— **仅 25 帧（10.5%）能完整看到 app**，**203 帧（85.7%）被 headed Edge 遮挡**；其中 s61-s64 Edge 停在 `about:blank` → **纯白、零文字**（white=83.2% / colored=0%；对照正常基线 white=0% / colored=5.1%），s268-s272 Edge 停在 Bing 结果页 → white=76.5%。**该画面与用户目击的「alias_agent 窗口整体变白、无文字」逐字吻合**。同轮 `IsHungAppWindow` **零次为真**（该 API 直接探测消息泵，**不受遮挡影响**）→ **app 自身未冻结**。故 12.1 的白屏观感由本项解释；**12.1 仍独立保留为 `[ ]`**（间歇性，未复现 ≠ 不存在）。
  **spec 合规性（已核，本项无需改 spec）**：spec L6 要求 "user-visible browser"；L30-31 要求 "SHALL NOT call per-step bring-to-front"；L20-21 明确支持窗口被最小化/**遮挡**时命令照常执行（task 1.2 / 1.5 已实证）。**把浏览器放到与 app 不重叠的位置**同时满足三者——仍然用户可见、且不 raise。
  **修复方向**：
  1. `sidecar/src/browser.cpp`：在 app 进程内取自身窗口矩形（`EnumWindows` + `GetCurrentProcessId()` + 类名 `FLUTTER_RUNNER_WIN32_WINDOW`），随 worker 启动参数/命令下发给 worker。
  2. `scripts/browser_worker.py`：headed launch（L169-170）加 `args=['--window-position=X,Y','--window-size=W,H']`；位置由「app 矩形 + 屏幕工作区」算出：优先放 app **右侧**，右侧不够放**左侧**，两侧都不够则退化为**最小重叠**并**如实记录**（不得静默退回全遮挡）。
  3. 算出的矩形必须 **clamp 进工作区**（含任务栏/DPI/多屏），不得把窗口放到屏幕外或负坐标。
  **验证**：
  - worker 层可自测：给定多组「app 矩形 × 屏幕工作区」→ 断言算出的浏览器矩形与 app 矩形**不相交**（退化档除外，且退化档必须留下明确记录）、且完全落在工作区内。落到 `test/integration/browser_worker_test.dart`（或 worker 内纯函数 + 对应层单测）。
  - 实跑：用已验证观测器（`%TEMP%\aliasagent-window-observer.ps1`，方法与基线见 12.1 新增段）跑 live，断言「**能完整看到 app 的帧占比**」显著高于 **10.5% 基线**——**阈值须实现后按实测确定，不得先挑一个好看的数字**。
  - `raise_count==0` 必须保持：本项**不得引入任何 bring-to-front**。
  **诚实边界**：`--window-position` / `--window-size` 在 headed + Playwright 下**是否被 Chromium 实际采纳属外部行为，必须实测确认，不得假设**（若被忽略，改用启动后经 Playwright/CDP 设置窗口 bounds 或换参数组合，并如实记录所用机制）；本项**只解决遮挡观感**，**不宣称解决 12.1 的间歇冻结**。
  **已实现（2026-09-11）**：
  - `sidecar/src/browser.cpp`：新增 `app_window_rect()`（`EnumWindows` + `GetCurrentProcessId()` + 类名 `FLUTTER_RUNNER_WIN32_WINDOW`，要求宽高 > 0）；`start_worker()` 把 `--app-rect=L,T,W,H` 追加进 worker 启动参数（取不到则不加，worker 退回 Playwright 默认放置）。**已编译通过**（`browser.cpp` → `sidecar.dll` / `sidecar_tests.exe`，无报错）。
  - `scripts/browser_worker.py`：新增**纯函数** `compute_window_bounds(app_rect, work_area)`（右侧 → 左侧 → 重叠退化，全部 clamp 进工作区）、`_get_work_area()`（`SystemParametersInfoW(SPI_GETWORKAREA)`，排除任务栏）、`_placement_launch_args()`、`_apply_window_bounds()`；headed launch 传 `args=...`，`new_page()` 后经 **CDP `Browser.setWindowBounds`** 精确落位。新增代码**全 ASCII**（该文件既有 54 个非 ASCII 字节属既有状况，非本次引入，数量未变）。
  **外部行为实测（关键，任务要求不得假设）**：写了一次性验证脚本（spawn 真 worker → 发 `open` → `EnumWindows` 读**真实窗口矩形**，而非只看传参）：
  - 第 1 轮（仅 launch flags）：**位置被采纳**（请求 x=1298 → 实得 1298），但 **`--window-size` 被忽略**（请求 750x1232 → 实得 **1296x808**），窗口右边缘达 2594 > 屏幕 2048 → **跑出屏幕 546px**（违反任务「不得放到屏幕外」）。
  - 第 2 轮（加 CDP 精确设界后）：**位置与尺寸都被采纳** —— 请求 `1298,0 750x1232`，实测窗口 `1298,0 750x1232`（worker 日志 `actual=1298,0 751x1232`），**与 app 重叠 0px**、右边缘 2048 恰好贴屏幕边。**此实测是本项结论的依据。**
  **测试**：`test/integration/browser_worker_test.dart` 新增用例「window placement avoids the app window and stays in the work area」（驱动纯函数、5 组场景、断言越界与重叠，并 `[OBS]` 打印每例结果）。**已跑过**：`+1 All tests passed`；`flutter analyze`（该测试文件 + worker）**No issues found**。C++ 侧 `sidecar_tests.exe "[browser]"` **15 断言 / 4 用例全过**。
  **尚未完成的验证（诚实边界）**：任务第 2 条实跑验证 —— **用观测器重跑 live，测「能完整看到 app 的帧占比」是否显著高于 10.5% 基线** —— 本轮尚在执行中，结果补记于下方；**该结果落地前本项不勾选**。

- [ ] 12.14 **诚实性审查（Workflow 对抗验证，round-8）**：对 12.13 修复跑 **Workflow 对抗验证**（N≥3 视角怀疑者、多数决 kill、离线、参照本地 docs+代码）。**必须携带 rounds 1-7 全部问题的修复状态做回归核对（rule 8）**：逐项验证 7.1-7.8/9.1-9.7/11.1-11.5/12.2-12.11 修复在**当前代码仍成立**（未被 12.13 破坏——尤其 12.13 **不得引入 bring-to-front** 从而违反 spec L30-31，且不得改动 snapshot/record 通道语义）。并核对：12.13 的遮挡证据是否真实（**亲自 Read** 观测脚本与帧统计，必要时重跑观测器核到）、位置计算是否**真被 Chromium 采纳**（**外部行为，必须有实测证据，不得采信"应该可以"**）、是否从根上消除而非换皮、是否未弱化任何既有验收、是否越出本 change 范围。发现问题按执行顺序插入修复任务，全部 `[x]` 后输出诚实性审查报告（已审查轮数、每轮问题数、最终任务状态）。
